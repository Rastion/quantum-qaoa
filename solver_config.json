{
    "entry_point": "qaoa_optimizer:QAOAOptimizer",
    "default_params": {
        "num_layers": 2,
        "max_iters": 100,
        "learning_rate": 0.1,
        "n_shots": 10000,
        "verbose": false
    },
    "optimizer_type": "QAOA",
    "description": "QAOA for QUBO problems. Internally requires the QUBO matrix of a qubot problem.",
    "compatible_problems": [
      "QUBO"
    ],
    
    "parameters": {
      "num_layers": {
        "type": "int",
        "description": "Number of layers for the QAOA quantum circuit."
      },
      "max_iters": {
        "type": "int",
        "description": "Maximum number of iterations for optimization process."
      },
      "learning_rate": {
        "type": "float",
        "description": "Learning rate for the quantum circuit optimization."
      },
      "n_shots": {
        "type": "int",
        "description": "Number of shots for the quantum circuit."
      },
      "verbose": {
        "type": "bool",
        "description": "Set to true if you want to see the optimization process in terminal output."
      }
    },
    "requirements": [
      "pennylane",
      "qubots",
      "numpy"
    ],
    "keywords": [
      "QUBO",
      "quantum optimization",
      "general QUBO solver",
      "pennylane"
    ]
}

